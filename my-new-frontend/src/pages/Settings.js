import React from 'react';
import { Typography } from '@mui/material';

const Settings = () => (
  <div>
    <Typography variant="h4" gutterBottom>Settings</Typography>
    <Typography>Settings options will appear here.</Typography>
  </div>
);

export default Settings; 